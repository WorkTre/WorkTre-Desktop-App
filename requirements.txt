# requirements.txt
# Core dependencies
pywebview>=4.0.0
pystray>=0.19.0
Pillow>=10.0.0
plyer>=2.0.0
requests>=2.31.0
cryptography>=41.0.0
packaging>=23.0.0
portalocker>=2.8.0

# Platform-specific (installed automatically on Windows)
pywin32>=306; sys_platform == 'win32'

# Optional for better UI
psutil>=5.9.0